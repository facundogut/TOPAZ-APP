EXECUTE('

IF OBJECT_ID (''dbo.BITACORA_TJC_MAESTRO_SALDO'') IS NOT NULL
	DROP TABLE dbo.BITACORA_TJC_MAESTRO_SALDO

')

EXECUTE('

CREATE TABLE dbo.BITACORA_TJC_MAESTRO_SALDO
	(
	ADMINISTRADORA VARCHAR (2) DEFAULT ((0)) NOT NULL,
	USUARIO        VARCHAR (20) DEFAULT ((0)) NOT NULL,
	MOTIVO_ERROR   VARCHAR (100) DEFAULT ('' ''),
	CONSTRAINT PK_BITACORA_TJC_MAESTRO_SALDO_01 PRIMARY KEY (ADMINISTRADORA, USUARIO)
	)

')