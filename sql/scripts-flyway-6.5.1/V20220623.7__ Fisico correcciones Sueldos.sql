EXECUTE('
ALTER TABLE CONV_DOMINIOS
ADD TIPO_DOMINIO          VARCHAR (1),
	SUB_DOMINIO           NUMERIC (4) NOT NULL DEFAULT 0,
	DESC_SUB_DOMINIO      VARCHAR (50),
	TIPO_SUB_DOMINIO      VARCHAR (1),
	RECIBO                VARCHAR (1) DEFAULT ''N'';
	
ALTER TABLE CONV_DOMINIOS
DROP CONSTRAINT PK_CONV_DOMINIOS;

ALTER TABLE CONV_DOMINIOS
ADD CONSTRAINT PK_CONV_DOMINIOS PRIMARY KEY (ID_CONVENIO, ID_DOMINIO, SUB_DOMINIO);
')

EXECUTE('
CREATE OR ALTER VIEW [VW_ORDEN_CODIGO_CORBANZA] (ORDEN,
									 CODIGOCOBRANZA,
									 CORRELATIVO,
									 TIPO,
									 CODIGOITEM)
AS 
SELECT C.ORDEN AS ''Orden'',
		C.CODIGO_COBRANZA AS ''CódigoCobranza'',
		C.CORRELATIVO AS ''Correlativo'',
		C.TIPO AS ''Tipo'',
		C.CODIGO_ITEM AS ''CodigoItem''
FROM dbo.CRE_COB_ORDEN_COBRANZA AS C WITH(NOLOCK);
')


