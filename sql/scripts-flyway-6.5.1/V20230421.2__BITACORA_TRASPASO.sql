EXECUTE('
IF OBJECT_ID (''dbo.BITACORA_TRASPASO'') IS NOT NULL
	DROP TABLE dbo.BITACORA_TRASPASO
')

EXECUTE('
CREATE TABLE dbo.BITACORA_TRASPASO
	(
	JTS_OID      NUMERIC (15) IDENTITY NOT NULL,
	FECHAPROCESO DATE NOT NULL,
	SUCURSAL     NUMERIC (10),
	ASIENTO      NUMERIC (10),
	OPERACION    NUMERIC (12) NOT NULL,
	JTS_ORIGEN   NUMERIC (15) NOT NULL,
	JTS_DESTINO  NUMERIC (15) NOT NULL,
	MONTO        NUMERIC (15, 2),
	PRIMARY KEY (JTS_OID)
	)
')