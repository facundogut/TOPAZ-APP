EXECUTE('
DECLARE @EXISTE NUMERIC(2)

SELECT @EXISTE = COUNT(1)
FROM INFORMATION_SCHEMA.COLUMNS
WHERE COLUMN_NAME = ''REVERSA'' AND TABLE_NAME = ''SNP_DEBITOS''

IF @EXISTE = 0
BEGIN
	ALTER TABLE SNP_DEBITOS
	ADD REVERSA NUMERIC (1);

END
')