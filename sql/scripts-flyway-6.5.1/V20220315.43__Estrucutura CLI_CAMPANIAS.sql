EXECUTE('
--
IF OBJECT_ID (''dbo.CLI_CAMPANIAS'') IS NOT NULL
	DROP TABLE dbo.CLI_CAMPANIAS
--
')

EXECUTE('
--
CREATE TABLE dbo.CLI_CAMPANIAS
	(
	TZ_LOCK                NUMERIC (15) DEFAULT ((0)) NOT NULL,
	PRODUCTO               NUMERIC (5),
	MONEDA                 NUMERIC (4),
	GRUPO                  VARCHAR (5) DEFAULT ('' ''),
	COD_CAMPANIA           NUMERIC (5) DEFAULT ((0)) NOT NULL,
	DESCRIPCION_CAMPANIA   VARCHAR (35) DEFAULT ('' ''),
	VIGENCIA_DESDE         DATETIME,
	VIGENCIA_HASTA         DATETIME,
	UTILIZACION            VARCHAR (1) DEFAULT ('' ''),
	PRODUCTO_PRE_EXISTENTE NUMERIC (5) DEFAULT ((-1)),
	PAQUETE_PRE_EXISTENTE  NUMERIC (5) DEFAULT ((-1)),
	RESIDENCIA             VARCHAR (1) DEFAULT ('' ''),
	SEGMENTO               NUMERIC (3) DEFAULT ((0)),
	SUBSEGMENTO            NUMERIC (3) DEFAULT ((0)),
	CANAL                  NUMERIC (1) DEFAULT ((0)),
	DEPENDENCIA            NUMERIC (3) DEFAULT ((0)),
	PAQUETE                NUMERIC (5) DEFAULT ((0)),
	WF_REDUCIDO            VARCHAR (1) DEFAULT (''0''),
	CONSTRAINT PK_CLI_CAMPANIAS PRIMARY KEY (COD_CAMPANIA)
	)
--
')

