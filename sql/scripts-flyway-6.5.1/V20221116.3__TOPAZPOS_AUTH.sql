EXECUTE('
IF OBJECT_ID (''dbo.SEQUENCE_POS_AUTHORIZATION'') IS NOT NULL
	DROP SEQUENCE dbo.SEQUENCE_POS_AUTHORIZATION


CREATE TABLE dbo.SEQUENCE_POS_AUTHORIZATION
	(
	id NUMERIC (18) IDENTITY NOT NULL
	)


')


EXECUTE('
create PROCEDURE [getSECUENCE_POS_AUTHORIZATION] 
		    @retorno numeric(18,0) out
	AS
	BEGIN
	    SET NOCOUNT ON;
		insert SEQUENCE_POS_AUTHORIZATION DEFAULT VALUES
		set @retorno = (SELECT IDENT_CURRENT(''SEQUENCE_POS_AUTHORIZATION'')) --(select @@identity as IDENT)
		delete from SEQUENCE_POS_AUTHORIZATION
	END
')
