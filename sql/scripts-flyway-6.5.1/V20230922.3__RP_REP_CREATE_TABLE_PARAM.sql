Execute('
DROP TABLE IF EXISTS ITF_RP_REPORTE_PARAMETROS;

CREATE TABLE ITF_RP_REPORTE_PARAMETROS (
	ID INT IDENTITY,
	CONVENIO VARCHAR(10),
	FECHA_REN VARCHAR(10),
	ARCHIVO_REN VARCHAR(50)
	CONSTRAINT PK_REP PRIMARY KEY (ID)
)
')