EXECUTE('
IF OBJECT_ID (''dbo.HISTORICO_CALIF_X_SALDO'') IS NOT NULL
	DROP TABLE dbo.HISTORICO_CALIF_X_SALDO
')

EXECUTE('
CREATE TABLE dbo.HISTORICO_CALIF_X_SALDO
	(
	JTS_PRESTAMO                     NUMERIC (10) DEFAULT ((0)) NOT NULL,
	FECHA                       DATETIME NOT NULL,
	CALIFICACION_OBJETIVA       VARCHAR (2) DEFAULT ('' ''),
	CALIFICACION_REESTRUCTURADA VARCHAR (2) DEFAULT ('' ''),
	TIPO_RIESGO                 VARCHAR (2) DEFAULT ('' ''),
	DIAS_ATRASO                 NUMERIC (6) DEFAULT ((0)),
	TZ_LOCK                     NUMERIC (15) DEFAULT ((0)) NOT NULL,
	CONSTRAINT PK_HISTORICO_CALIF_X_SALDO_01 PRIMARY KEY (JTS_PRESTAMO, FECHA)
	)
')

