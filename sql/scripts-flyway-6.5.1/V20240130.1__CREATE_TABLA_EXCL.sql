EXECUTE('

IF OBJECT_ID (''dbo.TLF_EXCLUSIONES_COD_TRANSACCIONES'') IS NOT NULL
	DROP TABLE dbo.TLF_EXCLUSIONES_COD_TRANSACCIONES
')

EXECUTE('
CREATE TABLE dbo.TLF_EXCLUSIONES_COD_TRANSACCIONES
	(
	COD_TLF  VARCHAR (6) NOT NULL,
	TRAN_TYP VARCHAR (1) NOT NULL,
	TZ_LOCK  NUMERIC (15),
	PRIMARY KEY (COD_TLF, TRAN_TYP)
	)
')