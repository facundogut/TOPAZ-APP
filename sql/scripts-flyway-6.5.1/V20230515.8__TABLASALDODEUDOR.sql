EXECUTE('
IF OBJECT_ID (''dbo.VTA_SEGURO_SALDO_DEUDOR'') IS NOT NULL
	DROP TABLE dbo.VTA_SEGURO_SALDO_DEUDOR
')

EXECUTE('
CREATE TABLE dbo.VTA_SEGURO_SALDO_DEUDOR
	(
	MAX_CUPO NUMERIC(15,2),
	SALDO_JTS_OID NUMERIC(10) NOT NULL,
	MONEDA NUMERIC(4),
	SALDOACTUAL NUMERIC(19,2),
	MONTO_SEGURO NUMERIC(15,2),
	TASA_SEGURO NUMERIC(11,7),
	FECHA DATETIME,
	ESTADO VARCHAR(1),
	CONSTRAINT PK_SEGURO_SALDO_DEUDOR PRIMARY KEY (SALDO_JTS_OID, FECHA)
	)
')