EXECUTE('
IF OBJECT_ID (''dbo.VTA_PENDIENTES_POS'') IS NOT NULL
	DROP TABLE dbo.VTA_PENDIENTES_POS
')

EXECUTE ('
CREATE TABLE dbo.VTA_PENDIENTES_POS
	(
	JTS_OID_CUENTA NUMERIC (15) DEFAULT ((0)) NOT NULL,
	FECHA          DATETIME NOT NULL,
	IMPORTE        NUMERIC (15, 2) DEFAULT ((0)),
	ESTADO         VARCHAR (1) DEFAULT ((0)),
	TZ_LOCK        NUMERIC (16) DEFAULT ((0)) NOT NULL,
	CONSTRAINT PK_PENDIENTES PRIMARY KEY (JTS_OID_CUENTA, FECHA)
	)

')