EXECUTE('
	IF OBJECT_ID (''dbo.RRII_MEDICION_LCR'') IS NOT NULL
	DROP TABLE dbo.RRII_MEDICION_LCR
')
EXECUTE('
CREATE TABLE dbo.RRII_MEDICION_LCR
	(
	CODIGOPARTIDA VARCHAR (12) NOT NULL,
	FECHA_INFORME DATETIME NOT NULL,
	FALAC_EET     NUMERIC (15),
	[SET]         NUMERIC (15),
	LCR           NUMERIC (15),
	VALOR         NUMERIC (2),
	COEFICIENTE   NUMERIC (4, 3),
	IMPORTE_MES1  NUMERIC (15),
	IMPORTE_MES2  NUMERIC (15),
	IMPORTE_MES3  NUMERIC (15),
	MONEDA        NUMERIC (5) NOT NULL,
	TZ_LOCK       NUMERIC (15) DEFAULT ((0)) NOT NULL,
	CONSTRAINT PK_MEDICION_LCR PRIMARY KEY (CODIGOPARTIDA, FECHA_INFORME, MONEDA)
	)
')
