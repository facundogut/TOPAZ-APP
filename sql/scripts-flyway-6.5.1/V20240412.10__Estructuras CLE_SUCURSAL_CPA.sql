EXECUTE('
IF OBJECT_ID (''dbo.CLE_SUCURSAL_CPA'') IS NOT NULL
	DROP TABLE dbo.CLE_SUCURSAL_CPA
')
EXECUTE('
CREATE TABLE dbo.CLE_SUCURSAL_CPA
	(
	TZ_LOCK   NUMERIC (15) DEFAULT ((0)) NOT NULL,
	ID        NUMERIC (5) DEFAULT ((0)) NOT NULL,
	ORDINAL   NUMERIC (3) DEFAULT ((0)) NOT NULL,
	CPA_VIEJO NUMERIC (4) DEFAULT ((0)),
	CONSTRAINT PK_CLE_SUCURSAL_CPA_01 PRIMARY KEY (ID, ORDINAL)
	)
')

