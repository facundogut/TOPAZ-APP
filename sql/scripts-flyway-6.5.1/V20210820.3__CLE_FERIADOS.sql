EXECUTE('
IF OBJECT_ID (''CLE_FERIADOS'') IS NOT NULL
	DROP TABLE CLE_FERIADOS


CREATE TABLE CLE_FERIADOS
	(
	Fecha      DATETIME NOT NULL,
	PI         NUMERIC (5) DEFAULT ((0)) NOT NULL,
	COD_POSTAL VARCHAR (15) NOT NULL,
	Localidad  VARCHAR (60) DEFAULT ('' '') NULL,
	Provincia  VARCHAR (60) DEFAULT ('' '') NULL,
	TZ_LOCK    NUMERIC (15) DEFAULT ((0)) NOT NULL,
	CONSTRAINT PK_CLE_FERIADOS_01 PRIMARY KEY (Fecha,PI,COD_POSTAL)
	)
')

