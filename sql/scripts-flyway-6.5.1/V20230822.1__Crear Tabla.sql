EXECUTE('
IF OBJECT_ID (''dbo.TMP_TRANSFERENCIAS_ORIGEN_DESTINO'') IS NOT NULL
	DROP TABLE dbo.TMP_TRANSFERENCIAS_ORIGEN_DESTINO

CREATE TABLE dbo.TMP_TRANSFERENCIAS_ORIGEN_DESTINO
	(
	FILA NUMERIC (15) IDENTITY NOT NULL,
	DATO VARCHAR (500),
	NOMBREARCHIVO VARCHAR(100),
	DES_ERROR VARCHAR(1000)
	)
')

EXECUTE('
IF OBJECT_ID (''dbo.TRANSFERENCIAS_ORIGEN_DESTINO'') IS NOT NULL
	DROP TABLE dbo.TRANSFERENCIAS_ORIGEN_DESTINO

CREATE TABLE dbo.TRANSFERENCIAS_ORIGEN_DESTINO
	(
	TIPO             VARCHAR (12) DEFAULT (''ORIGINANTE'') NOT NULL,
	PERIODO          DATE NOT NULL,
	LINEA            NUMERIC (15) IDENTITY NOT NULL,
	FIID_ORI         NUMERIC (4),
	PAN              NUMERIC (19),
	MEMBER           VARCHAR (3),
	FIID_DEST        NUMERIC (4),
	CTA_TIPO         VARCHAR (2),
	CTA_NRO          NUMERIC (19),
	CTA_ORIG         NUMERIC (19),
	TYP_MSJ          VARCHAR (4),
	TRAN_CDE         VARCHAR (6),
	RESP             VARCHAR (3),
	POST_DATE        NUMERIC (6),
	TRAN_DATE        NUMERIC (6),
	TRAN_TIME        NUMERIC (6),
	FIID_TER         VARCHAR (4),
	ID               VARCHAR (12),
	TYP_TER          VARCHAR (2),
	SEQ              VARCHAR (12),
	AMT              NUMERIC (10),
	MONEDA           NUMERIC (3),
	TIPO_CAMBIO      VARCHAR (10),
	TITULAR_CTA_DEST VARCHAR (1),
	CDI              NUMERIC (11),
	MOTIVO           VARCHAR (3),
	REFERENCIA       VARCHAR (12),
	NOMBRE           VARCHAR (22),
	DNI_DEST         NUMERIC (11),
	TIPO_CTA_ORIG    VARCHAR (2),
	AMT_EXT          VARCHAR (15),
	TERM_ID_16       VARCHAR (16),
	RAZON_SOC        VARCHAR (50),
	NOMBRE_FANT      VARCHAR (50),
	FILLER           VARCHAR (50),
	CONSTRAINT PK_TRANSF_ORIGEN_DESTINO PRIMARY KEY (TIPO, PERIODO, LINEA)
	)
')

EXECUTE('
IF OBJECT_ID (''dbo.TRANSFERENCIAS_CONCEPTOS_MOTIVOS'') IS NOT NULL
	DROP TABLE dbo.TRANSFERENCIAS_CONCEPTOS_MOTIVOS


CREATE TABLE dbo.TRANSFERENCIAS_CONCEPTOS_MOTIVOS
(
	CODIGO   VARCHAR (3) DEFAULT ('' '') NOT NULL,
	CONCEPTO VARCHAR (40) DEFAULT ('' ''),
	TZ_LOCK  NUMERIC (15) DEFAULT ((0)) NOT NULL,
	CONSTRAINT PK_TRANSFERENCIAS_CONCEPTOS_01 PRIMARY KEY (CODIGO)
)
')