execute('
IF OBJECT_ID (''dbo.ITF_RP_REPORTE_PARAMETROS'') IS NOT NULL
	DROP TABLE dbo.ITF_RP_REPORTE_PARAMETROS


CREATE TABLE dbo.ITF_RP_REPORTE_PARAMETROS
	(
	ID        INT IDENTITY NOT NULL,
	CONVENIO  VARCHAR (10),
	FECHA_REN VARCHAR (10),
	CONSTRAINT PK_REP PRIMARY KEY (ID)
	)

')
