EXECUTE('
IF OBJECT_ID (''dbo.VW_RRII_ANEXO_12'') IS NOT NULL
	DROP VIEW dbo.VW_RRII_ANEXO_12
')
EXECUTE('
CREATE   VIEW [dbo].[VW_RRII_ANEXO_12] AS

-- 1)
SELECT 
	1 AS RENGLON,
	
	COUNT(DISTINCT VR1.NUMEROPERSONA) AS COLUMNA1,
	
	VR1.FECHA AS FECHA,
	
	''S'' AS TIENE_FECHA

FROM VW_RRII_ANEXO_12_RENGLON1 VR1 WITH(NOLOCK)
GROUP BY 
	VR1.FECHA
		
--3) 
UNION ALL

SELECT 
	3 AS RENGLON, 
	
	COUNT(DISTINCT VR3.PERSONAS) AS COLUMNA1,
	
	VR3.FECHA AS FECHA,
	
	''S'' AS TIENE_FECHA

	FROM VW_RRII_ANEXO_12_RENGLON3 VR3 WITH(NOLOCK)
	GROUP BY 
		VR3.FECHA
	
UNION ALL
--4) SACAR DE VISTA SUMA ANEXO12 por NRO_CAMPO = 4 Y FECHA

SELECT	
	4 AS RENGLON, 
	
	SUM_CONT AS COLUMNA1, 
	
	FECHA AS FECHA, 
	
	''S'' AS TIENE_FECHA 
	
	FROM VW_RRII_TOTALES_ANEXO_12  WITH(NOLOCK) 
	
	WHERE NRO_CAMPO = 4


UNION ALL

SELECT  6 AS RENGLON,

		COUNT(DISTINCT(PERSONAS)) AS COLUMNA1,

		VR6.FECHA AS FECHA,

		''S'' AS TIENE_FECHA

	FROM VW_RRII_ANEXO_12_RENGLON6Y7 VR6 WITH(NOLOCK)
	WHERE MONTODEPOSITO<=(SELECT IMPORTE FROM PARAMETROSGENERALES WITH(NOLOCK) WHERE TZ_LOCK=0 AND CODIGO=738) 
	GROUP BY 
		VR6.FECHA
	

UNION ALL

SELECT  7 AS RENGLON,
		SUM(VR7.MONTODEPOSITO) AS COLUMNA1,
		VR7.FECHA AS FECHA,
		''S'' AS TIENE_FECHA
	FROM VW_RRII_ANEXO_12_RENGLON6Y7 VR7 WITH(NOLOCK)
	WHERE MONTODEPOSITO<=(SELECT IMPORTE FROM PARAMETROSGENERALES WITH(NOLOCK) WHERE TZ_LOCK=0 AND CODIGO=738) 
	GROUP BY 
		VR7.FECHA

UNION ALL
-- 8)
SELECT 
	8 AS RENGLON,
	SUM(VR8.MONTODEPOSITO) AS COLUMNA1,
	VR8.FECHA AS FECHA,
	''S'' AS TIENE_FECHA
	FROM VW_RRII_ANEXO_12_RENGLON3 VR8 WITH(NOLOCK)
	GROUP BY
		VR8.FECHA
	
--9) SE HACE CON FORMULA EN PLANTILLA EXCEL
UNION ALL

--10) VISTA SUMA_ANEXO12 NRO_CAMPO = 10
SELECT	
	10 AS RENGLON, 
	
	SUM_CONT AS COLUMNA1, 
	
	FECHA, 
	
	''S'' AS TIENE_FECHA 
FROM VW_RRII_TOTALES_ANEXO_12  WITH(NOLOCK) 
WHERE NRO_CAMPO = 10
UNION ALL
--11) IDEM ANTERIOR NRO_CAMPO = 11

SELECT 
	11 AS RENGLON,
	
	SUM_CONT AS COLUMNA1, 
	
	FECHA, 
	
	''S'' AS TIENE_FECHA 
FROM VW_RRII_TOTALES_ANEXO_12  WITH(NOLOCK)
WHERE NRO_CAMPO = 11

UNION ALL

SELECT 
		12 AS RENGLON,
		count(DISTINCT(g.SALDOS_JTS_OID)) AS COLUMNA1,
		G.FECHA AS FECHA,
		''S'' AS TIENE_FECHA		
	FROM GRL_SALDOS_DIARIOS G WITH(NOLOCK) 
	INNER JOIN SALDOS S WITH(NOLOCK) ON
		G.SALDOS_JTS_OID=S.JTS_OID AND
		G.MONEDA=S.MONEDA AND 
		G.TZ_LOCK=S.TZ_LOCK
	INNER JOIN CRE_GARANTIACREDITO CGC WITH(NOLOCK) ON 
		CGC.JTSOID_SALDO=G.SALDOS_JTS_OID AND
		CGC.TZ_LOCK=G.TZ_LOCK
	INNER JOIN CRE_GARANTIASRECIBIDAS CGR WITH(NOLOCK) ON
		CGR.NUM_GARANTIA = CGC.NUM_GARANTIA AND
		CGR.TZ_LOCK = G.TZ_LOCK
	WHERE CGR.TIPOGARANTIA=01 AND
		G.TZ_LOCK=0 AND 
		S.C1785=5 AND 
		G.MONEDA=(SELECT MONNAC FROM PARAMETROS WITH(NOLOCK)) AND
		G.SALDO_AJUSTADO!=0
	GROUP BY 
		G.FECHA

UNION ALL
--13) lo mismo que anterior pero con hipotecarios
SELECT 
		13 AS RENGLON,
		count(DISTINCT(g.SALDOS_JTS_OID)) AS COLUMNA1,
		G.FECHA AS FECHA,
		''S'' AS TIENE_FECHA		
	FROM GRL_SALDOS_DIARIOS G WITH(NOLOCK) 
	INNER JOIN SALDOS S WITH(NOLOCK) ON
		G.SALDOS_JTS_OID=S.JTS_OID AND
		G.MONEDA=S.MONEDA AND 
		G.TZ_LOCK=S.TZ_LOCK
	INNER JOIN CRE_GARANTIACREDITO CGC WITH(NOLOCK) ON 
		CGC.JTSOID_SALDO=G.SALDOS_JTS_OID AND
		CGC.TZ_LOCK=G.TZ_LOCK
	INNER JOIN CRE_GARANTIASRECIBIDAS CGR WITH(NOLOCK) ON
		CGR.NUM_GARANTIA = CGC.NUM_GARANTIA AND
		CGR.TZ_LOCK = G.TZ_LOCK
	WHERE CGR.TIPOGARANTIA=02 AND
		G.TZ_LOCK=0 AND 
		S.C1785=5 AND 
		G.MONEDA=(SELECT MONNAC FROM PARAMETROS WITH(NOLOCK)) AND
		G.SALDO_AJUSTADO!=0
	GROUP BY 
		G.FECHA
		
UNION ALL

--14) Cantidad de Operaciones por Otros Préstamos en Pesos.
SELECT 
		14 AS RENGLON,
		count(DISTINCT(g.SALDOS_JTS_OID)) AS COLUMNA1,
		G.FECHA AS FECHA,
		''S'' AS TIENE_FECHA		
	FROM GRL_SALDOS_DIARIOS G WITH(NOLOCK) 
	INNER JOIN SALDOS S WITH(NOLOCK) ON
		G.SALDOS_JTS_OID=S.JTS_OID AND
		G.MONEDA=S.MONEDA AND 
		G.TZ_LOCK=S.TZ_LOCK
	INNER JOIN CRE_GARANTIACREDITO CGC WITH(NOLOCK) ON 
		CGC.JTSOID_SALDO=G.SALDOS_JTS_OID AND
		CGC.TZ_LOCK=G.TZ_LOCK
	INNER JOIN CRE_GARANTIASRECIBIDAS CGR WITH(NOLOCK) ON
		CGR.NUM_GARANTIA = CGC.NUM_GARANTIA AND
		CGR.TZ_LOCK = G.TZ_LOCK
	WHERE CGR.TIPOGARANTIA NOT IN (01,02) AND
		G.TZ_LOCK=0 AND 
		S.C1785=5 AND 
		G.MONEDA=(SELECT MONNAC FROM PARAMETROS WITH(NOLOCK)) AND
		G.SALDO_AJUSTADO!=0
	GROUP BY 
		G.FECHA


UNION ALL
--15) Cantidad de Operaciones por Préstamos Prendarios en Moneda Extranjera.

SELECT 
		15 AS RENGLON,
		count(DISTINCT(g.SALDOS_JTS_OID)) AS COLUMNA1,
		G.FECHA AS FECHA,
		''S'' AS TIENE_FECHA		
	FROM GRL_SALDOS_DIARIOS G WITH(NOLOCK) 
	INNER JOIN SALDOS S WITH(NOLOCK) ON
		G.SALDOS_JTS_OID=S.JTS_OID AND
		G.MONEDA=S.MONEDA AND 
		G.TZ_LOCK=S.TZ_LOCK
	INNER JOIN CRE_GARANTIACREDITO CGC WITH(NOLOCK) ON 
		CGC.JTSOID_SALDO=G.SALDOS_JTS_OID AND
		CGC.TZ_LOCK=G.TZ_LOCK
	INNER JOIN CRE_GARANTIASRECIBIDAS CGR WITH(NOLOCK) ON
		CGR.NUM_GARANTIA = CGC.NUM_GARANTIA AND
		CGR.TZ_LOCK = G.TZ_LOCK
	WHERE CGR.TIPOGARANTIA=01 AND
		G.TZ_LOCK=0 AND 
		S.C1785=5 AND 
		G.MONEDA NOT IN (999,988,(SELECT MONNAC FROM PARAMETROS WITH(NOLOCK))) AND
		G.SALDO_AJUSTADO!=0
	GROUP BY 
		G.FECHA

UNION ALL

--16) Cantidad de Operaciones por Préstamos Hipotecarios en Mon. Extranjera.
SELECT 
		16 AS RENGLON,
		count(DISTINCT(g.SALDOS_JTS_OID)) AS COLUMNA1,
		G.FECHA AS FECHA,
		''S'' AS TIENE_FECHA		
	FROM GRL_SALDOS_DIARIOS G WITH(NOLOCK) 
	INNER JOIN SALDOS S WITH(NOLOCK) ON
		G.SALDOS_JTS_OID=S.JTS_OID AND
		G.MONEDA=S.MONEDA AND 
		G.TZ_LOCK=S.TZ_LOCK
	INNER JOIN CRE_GARANTIACREDITO CGC WITH(NOLOCK) ON 
		CGC.JTSOID_SALDO=G.SALDOS_JTS_OID AND
		CGC.TZ_LOCK=G.TZ_LOCK
	INNER JOIN CRE_GARANTIASRECIBIDAS CGR WITH(NOLOCK) ON
		CGR.NUM_GARANTIA = CGC.NUM_GARANTIA AND
		CGR.TZ_LOCK = G.TZ_LOCK
	WHERE CGR.TIPOGARANTIA=02 AND
		G.TZ_LOCK=0 AND 
		S.C1785=5 AND 
		G.MONEDA NOT IN (999,988,(SELECT MONNAC FROM PARAMETROS WITH(NOLOCK))) AND
		G.SALDO_AJUSTADO!=0
	GROUP BY 
		G.FECHA
UNION ALL
--17) Cantidad de Operaciones por otros Préstamos en Moneda Extranjera.
SELECT 
		17 AS RENGLON,
		count(DISTINCT(g.SALDOS_JTS_OID)) AS COLUMNA1,
		G.FECHA AS FECHA,
		''S'' AS TIENE_FECHA		
	FROM GRL_SALDOS_DIARIOS G WITH(NOLOCK) 
	INNER JOIN SALDOS S WITH(NOLOCK) ON
		G.SALDOS_JTS_OID=S.JTS_OID AND
		G.MONEDA=S.MONEDA AND 
		G.TZ_LOCK=S.TZ_LOCK
	INNER JOIN CRE_GARANTIACREDITO CGC WITH(NOLOCK) ON 
		CGC.JTSOID_SALDO=G.SALDOS_JTS_OID AND
		CGC.TZ_LOCK=G.TZ_LOCK
	INNER JOIN CRE_GARANTIASRECIBIDAS CGR WITH(NOLOCK) ON
		CGR.NUM_GARANTIA = CGC.NUM_GARANTIA AND
		CGR.TZ_LOCK = G.TZ_LOCK
	WHERE CGR.TIPOGARANTIA NOT IN (01,02) AND
		G.TZ_LOCK=0 AND 
		S.C1785=5 AND 
		G.MONEDA NOT IN (999,988,(SELECT MONNAC FROM PARAMETROS WITH(NOLOCK))) AND
		G.SALDO_AJUSTADO!=0
	GROUP BY 
		G.FECHA
UNION ALL
--18) VISTA SUMA_ANEXO12 NRO_CAMPO = 18
SELECT 
	18 AS RENGLON, 
	
	SUM(SUM_CONT) AS COLUMNA1, 
	
	FECHA, 
	
	''S'' AS TIENE_FECHA 
	
FROM VW_RRII_ANEXO_12_RENGLON18  WITH(NOLOCK)
GROUP BY 
	FECHA


UNION ALL
--19) IDEM ANTERIOR NRO_CAMPO = 19
SELECT 
	19 AS RENGLON, 
	
	SUM(SUM_CONT) AS COLUMNA1, 
	
	FECHA, 
	
	''S'' AS TIENE_FECHA 
	
FROM VW_RRII_ANEXO_12_RENGLON19  WITH(NOLOCK)
GROUP BY 
	FECHA
	

UNION ALL
--21) SE HACE CON FORMULA EN PLANTILLA EXCEL

SELECT 
	21 AS RENGLON, 
	
	SUM(SUM_CONT) AS COLUMNA1, 
	
	FECHA, 
	
	''S'' AS TIENE_FECHA 
	
FROM VW_RRII_ANEXO_12_RENGLON21  WITH(NOLOCK)
GROUP BY 
	FECHA
	
UNION ALL

--22) VISTA SUMA_ANEXO12 NRO_CAMPO = 22
SELECT 
	22 AS RENGLON, 
	
	SUM(SUM_CONT) AS COLUMNA1, 
	
	FECHA, 
	
	''S'' AS TIENE_FECHA 
	
FROM VW_RRII_ANEXO_12_RENGLON22  WITH(NOLOCK)
GROUP BY 
	FECHA
UNION ALL
--23) VISTA SUMA_ANEXO12 NRO_CAMPO =23, PERO FALTA PARAMETRIAS DE TIPOS DE CAJA DE AHORRO

SELECT 
	23 AS RENGLON, 
	
	SUM(SUM_CONT) AS COLUMNA1, 
	
	FECHA, 
	
	''S'' AS TIENE_FECHA 
	
FROM VW_RRII_ANEXO_12_RENGLON23  WITH(NOLOCK)
GROUP BY 
	FECHA
UNION ALL

--24) VISTA SUMA_ANEXO12 NRO_CAMPO = 24
SELECT 
	24 AS RENGLON, 
	
	SUM(SUM_CONT) AS COLUMNA1, 
	
	FECHA, 
	
	''S'' AS TIENE_FECHA 
	
FROM VW_RRII_ANEXO_12_RENGLON24 WITH(NOLOCK)
GROUP BY 
	FECHA

--25)
UNION ALL
	SELECT 25 AS RENGLON,
	
		NUM1 AS COLUMNA1,
		
		datefromparts(1900, 01, 01) AS FECHA,
		
		''N'' AS TIENE_FECHA
		
	FROM RRI_PARAMETROS_INF WITH (NOLOCK) 
	
		WHERE CODIGO=108 AND 
			TZ_LOCK=0 AND 
			ID1=25

UNION ALL
--26)

SELECT 26 AS RENGLON,
	
		NUM1 AS COLUMNA1,
		
		datefromparts(1900, 01, 01) AS FECHA,
		
		''N'' AS TIENE_FECHA
		
	FROM RRI_PARAMETROS_INF WITH (NOLOCK) 
	
		WHERE CODIGO=108 AND 
			TZ_LOCK=0 AND 
			ID1=26
--27)
UNION ALL
SELECT
	27 AS RENGLON,
	COUNT(NUMEROPERSONAFJ) AS COLUMNA1,
	G.FECHA AS FECHA,
	''S'' AS TIENE_FECHA
FROM GRL_SALDOS_DIARIOS G WITH (NOLOCK)
	
INNER JOIN SALDOS S WITH (NOLOCK) ON 
	G.SALDOS_JTS_OID = S.JTS_OID AND
	G.MONEDA = S.MONEDA AND
	G.TZ_LOCK = S.TZ_LOCK

INNER JOIN CLI_CLIENTEPERSONA CP WITH(NOLOCK) ON 
	CP.CODIGOCLIENTE = S.C1803 AND
	G.TZ_LOCK = CP.TZ_LOCK

INNER JOIN CLI_DocumentosPFPJ PFPJ WITH(NOLOCK) ON 
	CP.NUMEROPERSONA = PFPJ.NUMEROPERSONAFJ AND 
	G.TZ_LOCK = PFPJ.TZ_LOCK
WHERE
	S.C1785 = 4 AND
	PFPJ.TIPOPERSONA=''F'' AND
	G.TZ_LOCK=0 AND
	G.SALDO_AJUSTADO!=0
GROUP BY
	G.FECHA
-- 28) 
UNION ALL

SELECT
	28 AS RENGLON,
	COUNT(NUMEROPERSONAFJ) AS COLUMNA1,
	G.FECHA AS FECHA,
	''S'' AS TIENE_FECHA
FROM GRL_SALDOS_DIARIOS G WITH (NOLOCK)
	
INNER JOIN SALDOS S WITH (NOLOCK) ON 
	G.SALDOS_JTS_OID = S.JTS_OID AND
	G.MONEDA = S.MONEDA AND
	G.TZ_LOCK = S.TZ_LOCK

INNER JOIN CLI_CLIENTEPERSONA CP WITH(NOLOCK) ON 
	CP.CODIGOCLIENTE = S.C1803 AND
	G.TZ_LOCK = CP.TZ_LOCK

INNER JOIN CLI_DocumentosPFPJ PFPJ WITH(NOLOCK) ON 
	CP.NUMEROPERSONA = PFPJ.NUMEROPERSONAFJ AND 
	G.TZ_LOCK = PFPJ.TZ_LOCK
WHERE
	S.C1785 = 4 AND
	PFPJ.TIPOPERSONA=''J'' AND
	G.TZ_LOCK=0 AND
	G.SALDO_AJUSTADO!=0
GROUP BY
	G.FECHA
	
UNION ALL
--29)
SELECT 
	29 AS RENGLON,
	COUNT (M.ADMINISTRADORA) AS COLUMNA1,
	M.FECHA_GENERACION AS FECHA,
	''S'' AS TIENE_FECHA
FROM TJC_ADINTAR_DEUDASPLAS M WITH (NOLOCK)
WHERE 
	M.TZ_LOCK=0 AND
	M.CONDICION=''T''
GROUP BY 
	M.FECHA_GENERACION

UNION ALL
--30)
SELECT 
	30 AS RENGLON,
	COUNT (M.ADMINISTRADORA) AS COLUMNA1,
	M.FECHA_GENERACION AS FECHA,
	''S'' AS TIENE_FECHA
FROM TJC_ADINTAR_DEUDASPLAS M WITH (NOLOCK)
WHERE 
	M.TZ_LOCK=0 AND
	M.CONDICION=''A''
GROUP BY 
	M.FECHA_GENERACION
UNION ALL
--31)
SELECT 
	31 AS RENGLON,
	
	COUNT(DISTINCT(TJD.ID_TARJETA)) AS COLUMNA1,
	
	G.FECHA AS FECHA,
	
	''S'' AS TIENE_FECHA
		
FROM GRL_SALDOS_DIARIOS G WITH(NOLOCK) 
INNER JOIN TJD_REL_TARJETA_CUENTA TJD WITH(NOLOCK) ON
	G.SALDOS_JTS_OID=TJD.SALDO_JTS_OID AND 
	G.MONEDA=TJD.MONEDA AND 
	G.TZ_LOCK=TJD.TZ_LOCK AND
	G.SALDO_AJUSTADO!=0
WHERE 
	G.TZ_LOCK=0 AND 
	TJD.ESTADO=1
GROUP BY
	G.FECHA
	
UNION ALL
--32)
SELECT 
	32 AS RENGLON,
	COUNT(G.SALDOS_JTS_OID) AS COLUMNA1,
	G.FECHA AS FECHA,
	''S'' AS TIENE_FECHA 
FROM GRL_SALDOS_DIARIOS G WITH (NOLOCK)
INNER JOIN SALDOS S WITH (NOLOCK) ON
	G.SALDOS_JTS_OID = S.JTS_OID AND 
	G.MONEDA = S.MONEDA AND 
	G.TZ_LOCK = S.TZ_LOCK
WHERE 
	G.TZ_LOCK = 0 AND
	S.C1785 = 3 AND
	G.SALDO_AJUSTADO!=0 AND
	S.PRODUCTO IN 
		(SELECT NUM1 
		FROM RRI_PARAMETROS_INF WITH (NOLOCK) 
		WHERE CODIGO=109 AND
			ID1=32 
			AND TZ_LOCK=0)
GROUP BY 
	G.FECHA
UNION ALL
--33) 
SELECT 
	33 AS RENGLON,
	COUNT(G.SALDOS_JTS_OID) AS COLUMNA1,
	G.FECHA AS FECHA,
	''S'' AS TIENE_FECHA 
FROM GRL_SALDOS_DIARIOS G WITH (NOLOCK)
INNER JOIN SALDOS S WITH (NOLOCK) ON
	G.SALDOS_JTS_OID = S.JTS_OID AND 
	G.MONEDA = S.MONEDA AND 
	G.TZ_LOCK = S.TZ_LOCK
WHERE 
	G.TZ_LOCK = 0 AND
	S.C1785 = 3 AND
	G.SALDO_AJUSTADO!=0 AND
	S.PRODUCTO IN 
		(SELECT NUM1 
		FROM RRI_PARAMETROS_INF WITH (NOLOCK) 
		WHERE CODIGO=109 AND
			ID1=33 
			AND TZ_LOCK=0)
GROUP BY 
	G.FECHA
UNION ALL
--34)
SELECT 
	34 AS RENGLON,
	COUNT(G.SALDOS_JTS_OID) AS COLUMNA1,
	G.FECHA AS FECHA,
	''S'' AS TIENE_FECHA 
FROM GRL_SALDOS_DIARIOS G WITH (NOLOCK)
INNER JOIN SALDOS S WITH (NOLOCK) ON
	G.SALDOS_JTS_OID = S.JTS_OID AND 
	G.MONEDA = S.MONEDA AND 
	G.TZ_LOCK = S.TZ_LOCK
WHERE 
	G.TZ_LOCK = 0 AND
	S.C1785 = 3 AND
	G.SALDO_AJUSTADO!=0 AND
	S.PRODUCTO IN 
		(SELECT NUM1 
		FROM RRI_PARAMETROS_INF WITH (NOLOCK) 
		WHERE CODIGO=109 AND
			ID1=34 
			AND TZ_LOCK=0)
GROUP BY 
	G.FECHA
UNION ALL
--35)
SELECT 
	35 AS RENGLON,
	SUM(G.SALDO_AJUSTADO) AS COLUMNA1,
	G.FECHA AS FECHA,
	''S'' AS TIENE_FECHA 
FROM GRL_SALDOS_DIARIOS G WITH (NOLOCK)
INNER JOIN SALDOS S WITH (NOLOCK) ON
	G.SALDOS_JTS_OID = S.JTS_OID AND 
	G.MONEDA = S.MONEDA AND 
	G.TZ_LOCK = S.TZ_LOCK
WHERE 
	G.TZ_LOCK = 0 AND
	S.C1785 = 3 AND
	G.SALDO_AJUSTADO!=0 AND
	S.PRODUCTO IN 
		(SELECT NUM1 
		FROM RRI_PARAMETROS_INF WITH (NOLOCK) 
		WHERE CODIGO=109 AND
			ID1=34
			AND TZ_LOCK=0)
GROUP BY 
	G.FECHA
UNION ALL
--36)
SELECT 
	36 AS RENGLON,
	COUNT(G.SALDOS_JTS_OID) AS COLUMNA1,
	G.FECHA AS FECHA,
	''S'' AS TIENE_FECHA 
FROM GRL_SALDOS_DIARIOS G WITH (NOLOCK)
INNER JOIN SALDOS S WITH (NOLOCK) ON
	G.SALDOS_JTS_OID = S.JTS_OID AND 
	G.MONEDA = S.MONEDA AND 
	G.TZ_LOCK = S.TZ_LOCK
WHERE 
	G.TZ_LOCK = 0 AND
	S.C1785 = 3 AND
	G.SALDO_AJUSTADO!=0 AND
	S.PRODUCTO IN 
		(SELECT NUM1 
		FROM RRI_PARAMETROS_INF WITH (NOLOCK) 
		WHERE CODIGO=109 AND
			ID1=36
			AND TZ_LOCK=0)
GROUP BY 
	G.FECHA
	
UNION ALL
--37)
SELECT 
	37 AS RENGLON,
	SUM(G.SALDO_AJUSTADO) AS COLUMNA1,
	G.FECHA AS FECHA,
	''S'' AS TIENE_FECHA 
FROM GRL_SALDOS_DIARIOS G WITH (NOLOCK)
INNER JOIN SALDOS S WITH (NOLOCK) ON
	G.SALDOS_JTS_OID = S.JTS_OID AND 
	G.MONEDA = S.MONEDA AND 
	G.TZ_LOCK = S.TZ_LOCK
WHERE 
	G.TZ_LOCK = 0 AND
	S.C1785 = 3 AND
	G.SALDO_AJUSTADO!=0 AND
	S.PRODUCTO IN 
		(SELECT NUM1 
		FROM RRI_PARAMETROS_INF WITH (NOLOCK) 
		WHERE CODIGO=109 AND
			ID1=36
			AND TZ_LOCK=0)
GROUP BY 
	G.FECHA
	
UNION ALL

SELECT 
	38 AS RENGLON,
	COUNT(G.SALDOS_JTS_OID) AS COLUMNA1,
	G.FECHA AS FECHA,
	''S'' AS TIENE_FECHA 
FROM GRL_SALDOS_DIARIOS G WITH (NOLOCK)
INNER JOIN SALDOS S WITH (NOLOCK) ON
	G.SALDOS_JTS_OID = S.JTS_OID AND 
	G.MONEDA = S.MONEDA AND 
	G.TZ_LOCK = S.TZ_LOCK
WHERE 
	G.TZ_LOCK = 0 AND
	S.C1785 = 3 AND
	G.SALDO_AJUSTADO!=0 AND
	S.PRODUCTO IN 
		(SELECT NUM1 
		FROM RRI_PARAMETROS_INF WITH (NOLOCK) 
		WHERE CODIGO=109 AND
			ID1=38
			AND TZ_LOCK=0)
GROUP BY 
	G.FECHA

UNION ALL
--39)
SELECT 
	39 AS RENGLON,
	COUNT(G.SALDOS_JTS_OID) AS COLUMNA1,
	G.FECHA AS FECHA,
	''S'' AS TIENE_FECHA 
FROM GRL_SALDOS_DIARIOS G WITH (NOLOCK)
INNER JOIN SALDOS S WITH (NOLOCK) ON
	G.SALDOS_JTS_OID = S.JTS_OID AND 
	G.MONEDA = S.MONEDA AND 
	G.TZ_LOCK = S.TZ_LOCK
WHERE 
	G.TZ_LOCK = 0 AND
	S.C1785 = 3 AND
	G.SALDO_AJUSTADO!=0 AND
	S.PRODUCTO IN 
		(SELECT NUM1 
		FROM RRI_PARAMETROS_INF WITH (NOLOCK) 
		WHERE CODIGO=109 AND
			ID1=39 
			AND TZ_LOCK=0)
GROUP BY 
	G.FECHA

UNION ALL
--40)
SELECT 
	40 AS RENGLON,
	COUNT(G.SALDOS_JTS_OID) AS COLUMNA1,
	G.FECHA AS FECHA,
	''S'' AS TIENE_FECHA 
FROM GRL_SALDOS_DIARIOS G WITH (NOLOCK)
INNER JOIN SALDOS S WITH (NOLOCK) ON
	G.SALDOS_JTS_OID = S.JTS_OID AND 
	G.MONEDA = S.MONEDA AND 
	G.TZ_LOCK = S.TZ_LOCK
WHERE 
	G.TZ_LOCK = 0 AND
	S.C1785 = 3 AND
	G.SALDO_AJUSTADO!=0 AND
	S.PRODUCTO IN 
		(SELECT NUM1 
		FROM RRI_PARAMETROS_INF WITH (NOLOCK) 
		WHERE CODIGO=109 AND
			ID1=40
			AND TZ_LOCK=0)
GROUP BY 
	G.FECHA
UNION ALL
--41)
SELECT 
	41 AS RENGLON,
	COUNT(G.SALDOS_JTS_OID) AS COLUMNA1,
	G.FECHA AS FECHA,
	''S'' AS TIENE_FECHA 
FROM GRL_SALDOS_DIARIOS G WITH (NOLOCK)
INNER JOIN SALDOS S WITH (NOLOCK) ON
	G.SALDOS_JTS_OID = S.JTS_OID AND 
	G.MONEDA = S.MONEDA AND 
	G.TZ_LOCK = S.TZ_LOCK
WHERE 
	G.TZ_LOCK = 0 AND
	S.C1785 = 3 AND
	G.SALDO_AJUSTADO!=0 AND
	S.PRODUCTO IN 
		(SELECT NUM1 
		FROM RRI_PARAMETROS_INF WITH (NOLOCK) 
		WHERE CODIGO=109 AND
			ID1=41
			AND TZ_LOCK=0)
GROUP BY 
	G.FECHA
UNION ALL
--42)
SELECT 
	42 AS RENGLON,
	COUNT(G.SALDOS_JTS_OID) AS COLUMNA1,
	G.FECHA AS FECHA,
	''S'' AS TIENE_FECHA 
FROM GRL_SALDOS_DIARIOS G WITH (NOLOCK)
INNER JOIN SALDOS S WITH (NOLOCK) ON
	G.SALDOS_JTS_OID = S.JTS_OID AND 
	G.MONEDA = S.MONEDA AND 
	G.TZ_LOCK = S.TZ_LOCK
WHERE 
	G.TZ_LOCK = 0 AND
	S.C1785 = 3 AND
	G.SALDO_AJUSTADO!=0 AND
	S.PRODUCTO IN 
		(SELECT NUM1 
		FROM RRI_PARAMETROS_INF WITH (NOLOCK) 
		WHERE CODIGO=109 AND
			ID1=42
			AND TZ_LOCK=0)
GROUP BY 
	G.FECHA
UNION ALL
--43)
SELECT 
	43 AS RENGLON,
	COUNT(G.SALDOS_JTS_OID) AS COLUMNA1,
	G.FECHA AS FECHA,
	''S'' AS TIENE_FECHA 
FROM GRL_SALDOS_DIARIOS G WITH (NOLOCK)
INNER JOIN SALDOS S WITH (NOLOCK) ON
	G.SALDOS_JTS_OID = S.JTS_OID AND 
	G.MONEDA = S.MONEDA AND 
	G.TZ_LOCK = S.TZ_LOCK
WHERE 
	G.TZ_LOCK = 0 AND
	S.C1785 = 3 AND
	G.SALDO_AJUSTADO!=0 AND
	S.PRODUCTO IN 
		(SELECT NUM1 
		FROM RRI_PARAMETROS_INF WITH (NOLOCK) 
		WHERE CODIGO=109 AND
			ID1=43
			AND TZ_LOCK=0)
GROUP BY 
	G.FECHA
UNION ALL
--44)
SELECT 
	44 AS RENGLON,
	COUNT(G.SALDOS_JTS_OID) AS COLUMNA1,
	G.FECHA AS FECHA,
	''S'' AS TIENE_FECHA 
FROM GRL_SALDOS_DIARIOS G WITH (NOLOCK)
INNER JOIN SALDOS S WITH (NOLOCK) ON
	G.SALDOS_JTS_OID = S.JTS_OID AND 
	G.MONEDA = S.MONEDA AND 
	G.TZ_LOCK = S.TZ_LOCK
WHERE 
	G.TZ_LOCK = 0 AND
	S.C1785 = 3 AND
	G.SALDO_AJUSTADO!=0 AND
	S.PRODUCTO IN 
		(SELECT NUM1 
		FROM RRI_PARAMETROS_INF WITH (NOLOCK) 
		WHERE CODIGO=109 AND
			ID1=44
			AND TZ_LOCK=0)
GROUP BY 
	G.FECHA
UNION ALL
--45)
SELECT 
	45 AS RENGLON,
	SUM(G.SALDO_AJUSTADO) AS COLUMNA1,
	G.FECHA AS FECHA,
	''S'' AS TIENE_FECHA 
FROM GRL_SALDOS_DIARIOS G WITH (NOLOCK)
INNER JOIN SALDOS S WITH (NOLOCK) ON
	G.SALDOS_JTS_OID = S.JTS_OID AND 
	G.MONEDA = S.MONEDA AND 
	G.TZ_LOCK = S.TZ_LOCK
WHERE 
	G.TZ_LOCK = 0 AND
	S.C1785 = 3 AND
	G.SALDO_AJUSTADO!=0 AND
	S.PRODUCTO IN 
		(SELECT NUM1 
		FROM RRI_PARAMETROS_INF WITH (NOLOCK) 
		WHERE CODIGO=109 AND
			ID1=44
			AND TZ_LOCK=0)
GROUP BY 
	G.FECHA
')
