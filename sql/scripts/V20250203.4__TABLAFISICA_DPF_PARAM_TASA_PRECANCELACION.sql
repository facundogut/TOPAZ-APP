EXECUTE('
	IF OBJECT_ID (''dbo.DPF_PARAM_TASA_PRECANCELACION'') IS NOT NULL
		DROP TABLE dbo.DPF_PARAM_TASA_PRECANCELACION
')

EXECUTE('
	CREATE TABLE dbo.DPF_PARAM_TASA_PRECANCELACION
		(
		TZ_LOCK        		NUMERIC (15) NOT NULL,
		PRODUCTO            NUMERIC (5) DEFAULT ((0)) NOT NULL,
		MONEDA              NUMERIC (4) DEFAULT ((0)) NOT NULL,
		SECTOR				NUMERIC (2) DEFAULT ((0)) NOT NULL,
		PUNTOS_PORCENTUALES NUMERIC (11,7) DEFAULT ((0)) NOT NULL,
		FECHA				DATETIME NOT NULL,
		CONSTRAINT PK_DPF_PARAM_TASA PRIMARY KEY (PRODUCTO, MONEDA, SECTOR, FECHA)
		)
')