EXECUTE('
IF OBJECT_ID (''dbo.CRE_ADELANTOS_HABERES_RESUMEN'') IS NOT NULL
	DROP TABLE dbo.CRE_ADELANTOS_HABERES_RESUMEN

')

EXECUTE('

CREATE TABLE dbo.CRE_ADELANTOS_HABERES_RESUMEN
	(
	JTS_OID_CV      NUMERIC (15) NOT NULL,
	FECHA           DATETIME NOT NULL,
	IMPORTE         NUMERIC (15, 2),
	PRODUCTO        NUMERIC (5) NOT NULL,
	CUOTAS          NUMERIC (5),
	PERIODICIDAD    NUMERIC (5),
	CANAL           VARCHAR (5) NOT NULL,
	TZ_LOCK         NUMERIC (15),
	ESTADO          VARCHAR (1),
	NRO_RESERVA     NUMERIC (15),
	ID_CONVENIO     NUMERIC (15),
	CUOTA_CALCULADA NUMERIC (15, 2),
	CLIENTE         NUMERIC (12),
	FECHA_PROCESADO DATETIME,
	CONSTRAINT PK_CRE_ADELANTOS_HABERES_RESUMEN PRIMARY KEY (JTS_OID_CV, FECHA, PRODUCTO, CANAL)
	)


CREATE INDEX IDX_1
	ON dbo.CRE_ADELANTOS_HABERES_RESUMEN (TZ_LOCK, ESTADO, CLIENTE, PRODUCTO, CUOTA_CALCULADA)

')
