EXECUTE('
IF OBJECT_ID (''dbo.RML_BANDAS_ATM'') IS NOT NULL
	DROP TABLE dbo.RML_BANDAS_ATM;
')
EXECUTE('

CREATE TABLE dbo.RML_BANDAS_ATM
	(
	FECHA      DATETIME,
	PARTIDA    INT,
	MONEDA     NUMERIC (4),
	ATM        NUMERIC (5),
	SUCURSAL   NUMERIC (5),
	GRUPO_CAT  NUMERIC (2),
	UBICACION  VARCHAR (1),
	PONDERADOR NUMERIC (15, 2),
	DISPENSADO NUMERIC (15, 2),
	DIAS       INT,
	PROMEDIO   NUMERIC (26, 13),
	FRANQUICIA NUMERIC (38, 11)
	);
')