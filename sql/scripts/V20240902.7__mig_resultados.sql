drop table if exists dbo.MIG_RESULTADOS;

CREATE TABLE dbo.MIG_RESULTADOS
	(
	CICLO       NUMERIC (15),
	PROCESO     NUMERIC (15),
	MODULO      VARCHAR (15),
	FECHAINICIO DATETIME,
	FECHAFIN    DATETIME,
	ESTADO      VARCHAR (1),
	ERRORES     VARCHAR (1),
	MENSAJE     VARCHAR (1000),
	ARCHIVO     VARCHAR (15)
	)
GO

