EXECUTE('
IF OBJECT_ID (''dbo.GRL_CAB_ENVIO_ESTCTA'') IS NOT NULL
	DROP TABLE dbo.GRL_CAB_ENVIO_ESTCTA
')
EXECUTE('
CREATE TABLE dbo.GRL_CAB_ENVIO_ESTCTA
	(
	LEGAL               CHAR (1) NOT NULL,
	PERIODO             CHAR (1) NOT NULL,
	IDCAB               NUMERIC (12) NOT NULL,
	CODIGOCLIENTE       NUMERIC (12) NOT NULL,
	TIPODIRECCION       VARCHAR (2) NOT NULL,
	NOMBRECLIENTE       VARCHAR (70),
	CALLE               VARCHAR (60),
	NUMPUERTA           VARCHAR (10),
	APARTAMENTO         VARCHAR (5),
	PISO                NUMERIC (3),
	CIUDADLOCALIDAD     VARCHAR (60),
	CODIGOPAIS          NUMERIC (5),
	CODIGOPOSTAL        VARCHAR (15),
	FECHAEMISION        DATETIME,
	PROCESADO           VARCHAR (1),
	SUCURSAL            NUMERIC (5),
	NOMBRESUCURSAL      VARCHAR (35),
	FECHADESDE          DATETIME,
	FECHAHASTA          DATETIME,
	CUENTA              NUMERIC (12),
	JTSOID              NUMERIC (10) NOT NULL,
	TIPOPRODUCTO        NUMERIC (5),
	TIPOCUENTA          VARCHAR (60),
	MONEDA              VARCHAR (20),
	RESUMENEN           VARCHAR (20),
	CUITCUIL            VARCHAR (20),
	CBU                 VARCHAR (23),
	SALDOINI            NUMERIC (15, 2),
	CANTTITULARES       NUMERIC (10),
	NOMBRETIT           VARCHAR (70),
	DOCUMENTOTIT        VARCHAR (20),
	NOMBRETIT2          VARCHAR (70),
	DOCUMENTOTIT2       VARCHAR (20),
	NOMBRETIT3          VARCHAR (70),
	DOCUMENTOTIT3       VARCHAR (20),
	BARRIO              VARCHAR (100),
	PROVINCIA           VARCHAR (60),
	FECHA_CANCELACION   DATETIME,
	TIPO_CAMBIO_OFICIAL NUMERIC (15, 9),
	COD_PRODUCTO        NUMERIC (5),
	COD_MONEDA          NUMERIC (4),
	SALDO_FINAL         NUMERIC (15, 2),
	SIRCREB             NUMERIC (15, 2),
	IIBB_CORRIENTES     NUMERIC (15, 2),
	CONSTRAINT PK_GRL_CAB_ENVIO_ESTCTA PRIMARY KEY (LEGAL, PERIODO, JTSOID)
	)
')
EXECUTE('
CREATE INDEX IDX_ESTADOCUENTA
	ON dbo.GRL_CAB_ENVIO_ESTCTA (JTSOID)
')

