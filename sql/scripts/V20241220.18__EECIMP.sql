EXECUTE('
IF OBJECT_ID (''dbo.GRL_ECC_CREDEB'') IS NOT NULL
	DROP TABLE dbo.GRL_ECC_CREDEB
')

EXECUTE('
CREATE TABLE dbo.GRL_ECC_CREDEB
	(
	SALDO_JTS_OID             NUMERIC (10) DEFAULT ((0)) NOT NULL,
	FECHA_DESDE               DATETIME NOT NULL,
	FECHA_HASTA               DATETIME NOT NULL,
	NUMERICO             	  BIGINT NOT NULL,
	ALICUOTA           		  VARCHAR (500),
	TASA_CREDITO_FISCAL       VARCHAR (500),
	TOTAL_PERCIBIDO			  NUMERIC (15,2),
	CREDITO_COMPUTABLE		  NUMERIC(15,2),
	CONSTRAINT PK_GRL_ECC_CREDEB PRIMARY KEY (SALDO_JTS_OID, FECHA_DESDE, FECHA_HASTA, NUMERICO)
	)
')