EXECUTE('
----
IF OBJECT_ID (''dbo.CON_REGIMEN_CERTIF_RET'') IS NOT NULL
	DROP TABLE dbo.CON_REGIMEN_CERTIF_RET
--
IF OBJECT_ID (''dbo.CON_EQUIVALENCIA_CARGO_IMPOSITIVO'') IS NOT NULL
	DROP TABLE dbo.CON_EQUIVALENCIA_CARGO_IMPOSITIVO
----
')
EXECUTE('
----
CREATE TABLE dbo.CON_REGIMEN_CERTIF_RET
	(
	TZ_LOCK               	NUMERIC (15) DEFAULT ((0)) NOT NULL,
	CODIGO_REGIMEN        	NUMERIC (4) DEFAULT ((0)),
	TIPO_CARGO_IMPOSITIVO 	NUMERIC (2) DEFAULT ((0)) NOT NULL,
	VIGENCIA_DESDE        	DATETIME NOT NULL,
	DESCRIPCION_REGIMEN   	VARCHAR (100) DEFAULT ('' ''),
	NOTACION_REGIMEN		VARCHAR (40) DEFAULT ('' ''),
	CONSTRAINT PK_CON_REGIMEN_CERTIF_RET_01 PRIMARY KEY (TIPO_CARGO_IMPOSITIVO, VIGENCIA_DESDE)
	)
--
CREATE TABLE dbo.CON_EQUIVALENCIA_CARGO_IMPOSITIVO
	(
	TZ_LOCK               NUMERIC (15) DEFAULT ((0)) NOT NULL,
	TIPO_IMPUESTO         NUMERIC (2) DEFAULT ((0)) NOT NULL,
	CARGO_O_IMPUESTO      VARCHAR (1) DEFAULT ('' '') NOT NULL,
	TIPO_CARGO_IMPOSITIVO NUMERIC (2) DEFAULT ((0)),
	DESCRIPCION           VARCHAR (40) DEFAULT ('' ''),
	CONSTRAINT PK_CON_EQUIVALENCIA_CARGO_I_01 PRIMARY KEY (TIPO_IMPUESTO, CARGO_O_IMPUESTO)
	)
----
')

