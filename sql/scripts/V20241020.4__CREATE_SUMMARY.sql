EXECUTE('
IF OBJECT_ID (''dbo.TJD_TLF_SUMMARY'') IS NOT NULL
	DROP TABLE dbo.TJD_TLF_SUMMARY

')

EXECUTE('
CREATE TABLE dbo.TJD_TLF_SUMMARY
	(
	JTS_OID              NUMERIC (18) IDENTITY NOT NULL,
	TIPOMENSAJE          VARCHAR (4) NOT NULL,
	CODIGOPROCESAMIENTO  VARCHAR (6) NOT NULL,
	FECHACAPTURA         VARCHAR (4) NOT NULL,
	FECHATRANSACCION     VARCHAR (4) NOT NULL,
	HORATRANSACCION      VARCHAR (6) NOT NULL,
	CODIGORESPUESTA      VARCHAR (2),
	NUMEROTARJETA        VARCHAR (30),
	NUMEROTRANSACCION    VARCHAR (12) NOT NULL,
	NUMEROAUTORIZACION   VARCHAR (6),
	IDENTIFICACIONCAJERO VARCHAR (16) NOT NULL,
	IMPORTE              VARCHAR (15),
	IMPORTEDISPENSADO    VARCHAR (12),
	FROMACCOUNT          VARCHAR (21),
	TOACCOUNT            VARCHAR (21),
	MONEDA               VARCHAR (1),
	CONCILIACION         VARCHAR (1),
	IMPORTETLF           NUMERIC (15, 2),
	CONSTRAINT TJD_TLF_SUMMARY_PK PRIMARY KEY (JTS_OID)
	)
')

EXECUTE(' 
CREATE INDEX IX_TJD_TLF_SUMMARY
	ON dbo.TJD_TLF_SUMMARY (TIPOMENSAJE, CODIGOPROCESAMIENTO, FECHACAPTURA, FECHATRANSACCION, HORATRANSACCION, NUMEROTRANSACCION, IDENTIFICACIONCAJERO)
')