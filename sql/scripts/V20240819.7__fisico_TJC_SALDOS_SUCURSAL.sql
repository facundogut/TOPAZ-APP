EXECUTE('
IF OBJECT_ID (''TJC_SALDOS_SUCURSAL'') IS NOT NULL
	DROP TABLE TJC_SALDOS_SUCURSAL


CREATE TABLE TJC_SALDOS_SUCURSAL
	(
	SUCURSAL      NUMERIC (5) DEFAULT ((0)) NOT NULL,
	PERIODO_DEUDA NUMERIC (6) DEFAULT ((0)) NOT NULL,
	TOTAL_DEUDA   NUMERIC (19, 2) DEFAULT ((0)),
	TZ_LOCK       NUMERIC (15) DEFAULT ((0)) NOT NULL,
	CONSTRAINT PK_TJC_SALDOS_SUCURSAL_01 PRIMARY KEY (SUCURSAL)
	)
')

